var sessions = require('sessions');

module.exports.process = function(req, res) {
	sessions.isOpen(req, res); //if sessions is not open for this request will redirect to auth.html
	res.end("books");
}

