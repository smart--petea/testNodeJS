var sessions = require('sessions'),
	util1 = require('util1');

/* exports */
module.exports.process = process;

/* functions */
function process(req, res){
	//destroy session
	sessions.close(req);
	util1.redirectTo(res, "/auth");
}
